import 'package:flutter/material.dart';

class BelajarAngka extends StatelessWidget {
  const BelajarAngka({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('ini belajar angka'),
      ),
    );
  }
}